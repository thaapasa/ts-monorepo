{
  "name": "server",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "rm -rf dist",
    "start": "nodemon"
  },
  "dependencies": {
    "debug": "*"
  },
  "devDependencies": {
    "@swc-node/register": "*",
    "@types/debug": "*",
    "nodemon": "*",
    "tsconfig-paths": "*",
    "typescript": "*"
  }
}
