{
  "name": "server",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "rm -rf dist",
    "start": "nodemon"
  },
  "dependencies": {
    "debug": "*"
  },
  "devDependencies": {
    "@types/debug": "*",
    "nodemon": "*",
    "ts-node": "*",
    "tsconfig-paths": "*",
    "typescript": "*"
  }
}
